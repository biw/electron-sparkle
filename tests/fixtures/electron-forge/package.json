{
  "name": "electron-sparkle-forge-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "electron-sparkle Electron Forge integration fixture",
  "main": "main.cjs",
  "scripts": {
    "typecheck": "tsc --ignoreConfig --noEmit --strict --skipLibCheck --module NodeNext --moduleResolution NodeNext --target ES2022 config-types.ts"
  },
  "dependencies": {
    "electron-sparkle": "file:../../../release/electron-sparkle-0.2.0.tgz"
  },
  "devDependencies": {
    "@electron-forge/cli": "7.11.2",
    "electron": "43.4.1",
    "typescript": "7.0.2"
  },
  "config": {
    "forge": "./forge.config.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.22.0"
}
